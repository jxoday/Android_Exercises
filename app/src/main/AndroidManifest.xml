<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jinxin.exercises">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Layout.TableLayout" />
        <activity android:name=".Layout.FrameLayout_a" />
        <activity android:name=".Layout.FrameLayout_b" />
        <activity android:name=".Layout.GridLayout" />
        <activity android:name=".Widget.TextView_a">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Layout.ConstraintLayout"></activity>
    </application>

</manifest>